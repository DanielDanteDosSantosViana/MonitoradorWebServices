var monitorweb = function (options){
/*
    var captor = require('./parser/captor');
    var properties = require('./config/properties_server');
    var agentConnection  = require('./connection/AgentSocket')();

    agentConnection.connect(properties, function(error,msg){
      if(error!=null){
          process.stdout.write(msg);
          process.exit(1);
      }

      captor(param,agentConnection);

    });
*/
}

module.exports = monitorweb;
